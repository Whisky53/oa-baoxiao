---
name: oa-baoxiao
description: 差旅费报销自动化：识别票据、填写企业 OA 差旅费报销单，并整理脱敏问题反馈。支持批量识别发票/行程单/自驾Excel，只暂存不提交。
agent_created: true
---

# oa-baoxiao

## 触发条件

- 报销：用户说“报销差旅费用”或“提交差旅报销”，或明确要求识别票据并填写 OA 差旅费报销单。
- 问题反馈：用户说“差旅报销问题反馈”。反馈任务不得执行报销填单。

## 环境配置（仅新环境，一次性；先配置，再运行）

Skill 本体不含任何机构私有的 `config.json`。每个新环境首次使用前必须完成环境配置，
后续每次报销直接走「运行流程」，不再重复配置。

1. 提供本机配置 `config.json`（放真实机构参数，不入仓库，不入分发）：字段结构可参照 `config.example.json`（匿名示例，`OA_HOST`/`SSO_HOST`/`TEMPLATE_ID` 为占位符）：
   - `oa.portal` / `oa.login_url` / `oa.reimburse_new_url`：OA 门户、统一认证登录页、报销流程模板地址；
   - `company` / `fee_type`：报销公司名与默认费用类型；
   - `project`、`invoice_dir`：可为 null，运行时用 `--project` 与票据目录参数指定。
2. 安装依赖并登录：运行 `python install.py` 完成依赖安装与 OA 登录会话保存（会话只存本机）。
3. 自检通过（能打开 OA 应用页）即完成环境配置。

注意：`data/oa_state.json` 为登录会话，只保存在本机，不纳入分发、不提交仓库。

### 会话获取（跨平台方法矩阵，先看平台再选）

OA 登录会话必须保存为 `data/oa_state.json`（Playwright storage_state）。三种获取方式：

| 平台 | 首选方法 | 命令 | 说明 |
|---|---|---|---|
| **macOS** | Chrome cookie 自动提取 | `python scripts/chrome_cookies_to_state.py` | 读本机 Chrome 里已登录 OA 的 cookie（经 Keychain 解密），无需重新输密码。依赖本机 Chrome 已登录过 OA。 |
| **Windows/Linux** | 浏览器手动登录引导 | `python scripts/login_browser.py` | 弹出浏览器 → 用户输账号登录 OA → 自动保存会话。`install.py` 会自动调用本方法（macOS 提取失败时也回落此方法）。 |

**初始化登录常见坑（Windows + SSO 实测）**：
- `login_browser.py` 用 playwright 的**全新独立浏览器 context** 打开门户，若该 context 无任何登录态，会先被重定向到 SSO 登录页（如 idaas）。在 SSO 页登录后**必须真正跳回 OA 域**才算成功；若登录后 URL 仍停在 SSO 页（含 `login`），脚本判定失败、不保存会话。
- **SSO 卡住时的关键解法**：让 playwright 启动的 context 复用"已有 OA 登录态"的 Chrome（macOS 走 cookie 提取；Windows 若本机某 Chrome profile 已登录 OA，可改走 CDP 或让其上下文继承），并采用**宽松判定**——只要离开 SSO 登录页、落在 OA 所在域（含公司多子域）即判定成功，不要死等 host 完全相等。
- 会话自检标准：无头打开报销单 URL，未被重定向到登录页、且发票/其它附件控件可访问 = 会话有效。

### 依赖与运行环境
- 依赖：`playwright` + `pypdf`（自驾 Excel 另需 `openpyxl`）。建议装进**隔离 venv**，避免污染系统 Python。
- Python 3.8+ 即可。

## 运行流程

执行前确认：本次报销项目、票据目录为同一批次。然后：

1. 扫描票据目录内全部非隐藏文件，动态识别发票与其它附件，不写死票据类型或数量。
2. 真实发票上传到“发票”栏；行程单、登机凭证、水单、地图、Excel 等上传到“其它附件”栏。
3. 发票上传后等待 OA 自动解析明细；普通票据不重复添加行，有专项规则的票据按专项规则处理。
4. 填写项目、费用类型、日期、地点、金额、发票号码等字段。
5. 点击“暂存”前，必须先按下方「暂存前检查清单」逐项核对整单内容，全部通过后才可点击“暂存”；不得点击“提交”。只有取得明确的“已点暂存”回执，才可报告成功。

### 首次真实填单（节奏与判定，实测）
- **整单需数分钟**：上传发票 → 等 OA 自动解析明细（约≤45s）→ 逐行填费用类型/日期/金额/发票号 → 选项目。期间 stdout 可能长时间无新增，属正常等待，**勿过早判定卡死**。
- 填完自驾行程表后日志会停在“自驾行程表已上传”，其后在等 OA 解析+逐行填录，可能数分钟无输出。
- 判成功标准：出现 `暂存结果: OK: 已点暂存`。**暂存后仍需用户在浏览器窗口人工核对整单**（发票是否全解析、明细/金额/日期是否对），确认无误才可报完成。有头模式填完会保持窗口约 20 分钟供核对。
- 该流程**只暂存不提交**；提交需用户在 OA 里对已存草稿手动操作。

## 票据规则

### 通用

- 发票栏文件数 = 去重后的发票数 = OA 表单发票张数；不一致时停止，不暂存。
- 火车票为专票；滴滴、机票、酒店、自驾发票为普票。
- 无法确认是否为发票时放入其它附件并提示人工确认，不得强行计入发票。
- PDF 无法解析但其文件名含 20 位连续数字时，按发票保留并提示人工补全字段。

### 滴滴

- 电子发票与对应行程报销单按金额、行程等信息匹配。
- 行程报销单只上传到其它附件，不得上传到发票栏。
- 开始日期和结束日期必须分别取行程报销单的“行程起止日期”，写入 OA 对应字段，不能只写乘车日期。
- 日期为空时禁止暂存；费用类型使用“出租车/网约车费/滴滴”。
- 企业滴滴、跨市打车、额度或凭证不符合制度时，提示补充凭证或审批，不得静默视为合规。

### 自驾

- 文件名含“自驾发票”的发票只上传一次，到发票栏。
- 自驾行程表 Excel 只上传到“自驾行程表”专用附件控件，**不得进入“其它附件”栏**；同一份 Excel 在两栏都出现视为重复上传，禁用暂存。
- Excel 只读取同时具备有效日期、出发地、到达地和金额的业务行，排除空行和表尾说明行。
- 每条有效 Excel 业务行对应 OA 一条明细，填写出发地、到达地、日期、公里数、原因和金额；所有明细共用同一发票号码。
- 若 OA 自动只生成一条明细，必须新增至有效 Excel 行数；明细数不一致时禁止暂存。
- 发票价税合计可大于报销总额；按 Excel 明细金额填写即可。

### 酒店、机票及其它

- 酒店日期以水单为准，费用类型为住宿。
- 机票发票号码兼容“发票号码”标签与号码分行的版式。
- 非发票凭证统一进入其它附件栏。

## 暂存前检查清单

点击“暂存”前必须逐项核对以下内容；任一项不通过即停止，先修复或人工确认，禁止暂存：

1. 数量一致：发票栏文件数 = 去重发票数 = 表单“发票张数”；发票数与明细行结构一致。
2. 明细完整：自驾明细数 = Excel 有效业务行数；其它每张发票都有对应明细行。
3. 字段完整：每行费用类型、项目、开始/结束日期、出发地/到达地、金额、发票号码均已填写且与来源一致。
4. 滴滴：开始日期、结束日期取自行程报销单“行程起止日期”，两栏均不为空；行程单在其它附件栏，不在发票栏。
5. 自驾：所有明细共用同一发票号码，金额按 Excel 业务行填写，合计不超过发票价税合计；地点、日期、公里数、原因与 Excel 一致。
6. 票型匹配：火车票=专票且费用类型为火车；滴滴=普票、机票=普票、酒店=普票且费用类型为住宿、自驾=普票，均与票据内容一致。
7. 附件分流：发票栏只含真实发票；行程单、登机凭证、水单、地图、Excel 等凭证均在其它附件栏；**自驾行程表 Excel 必须只出现在“自驾行程表”专用控件，不允许同时出现在其它附件栏**。
8. 页面无拦截提示：如出现“资金计划剩余金额小于本次申请金额”等校验提示，停止并请求人工确认，不得强行通过。

## 配置与运行

- 配置文件：`config.json`（本机提供，含真实机构参数，不入仓库、不入分发）；`config.example.json` 为匿名示例（字段结构参考，install.py 不依赖它）。
- 配置优先级：命令行参数 > `config.json` > 人工确认。
- 批量运行：`python scripts/fill_oa_batch.py "票据目录" --project "项目名"`
- 单票运行：`python scripts/fill_oa_auto.py "发票.pdf" --project "项目名"`
- 可选参数：`--trip`、`--headless`、`--no-save`；会话文件可通过 `OA_STATE_FILE` 指定。
- 用哪套 Python：建议先建隔离 venv 再装依赖，之后运行脚本统一用该 venv 的 python（避免污染系统、保证 playwright 可用）。Windows 也接受 `py -3` / `python`；路径含空格时加引号。
- **Chrome 探测的平台差异（重要）**：`fill_oa_auto.py`/`fill_oa_batch.py` 的 `detect_chrome()` 探测路径——
  - macOS：`/Applications/Google Chrome.app`；
  - Windows：仅 `C:\Program Files\Google Chrome` 与 `C:\Program Files (x86)\...`（**不含用户级 `%LOCALAPPDATA%\Google\Chrome`**）+ agent-browser 目录。
  - 若本机 Chrome 是**用户级安装**（装在 `%LOCALAPPDATA%`），会报 `FileNotFoundError: 未找到 Chrome`。此时必须设环境变量指定路径，Windows 形如：
    `CHROME="%LOCALAPPDATA%\Google\Chrome\Application\chrome.exe" python scripts/fill_oa_batch.py "票据目录" --project "项目名"`
  - 注意 `install.py` 的 `detect_chrome()` 已含 `LOCALAPPDATA`，而 `fill_oa_*.py` 未含——两者不一致，填单脚本必须显式给 `CHROME`。
- **后台/重定向运行务必加 `python -u`（无缓冲）**，否则脚本 stdout 被块缓冲，看不到实时解析与填单日志，
  易误判为卡死。脚本填单需数分钟，日志可能长时间停在某一步（如“自驾行程表已上传”），属正常等待，勿过早终止。
- 会话引导在 macOS 可自动提取本机 Chrome 登录态，Windows/Linux 使用
  `login_browser.py` 打开浏览器手动登录一次。

## 浏览器与安全

- SSO 登录后必须进入 OA 应用页面；停留在统一认证门户不算登录完成。
- 优先连接用户已登录且开启调试端口的 Chrome；无法连接时提示用户启动带远程调试参数的普通版 Chrome：
  - macOS：`"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --remote-debugging-port=9222 --user-data-dir="$HOME/chrome-debug-profile"`
  - Windows：`start "" "C:\Program Files\Google Chrome\Application\chrome.exe" --remote-debugging-port=9222 --user-data-dir="%USERPROFILE%\chrome-debug-profile"`
  - 随后通过 `OA_CDP_URL=http://127.0.0.1:9222` 连接该浏览器（沿用其登录会话，无需重复输入账号）。
- 只有确认 OA 新建报销单页面已加载，且发票、其它附件控件存在后，才开始上传。
- 不保存或输出密码、Cookie、Token、姓名、身份证号、发票号码和完整行程等敏感信息。
- 不自动提交报销单；不确定、字段缺失或校验失败时停止并请求人工确认。

## 问题反馈

- 仅记录脱敏后的可复现技术问题，不上传原始对话、截图、会话文件、Cookie、Token、密码、个人绝对路径或个人身份信息。
- 反馈格式：`日期｜分类｜现象｜影响｜复现条件｜处理结论｜待验证｜状态`。
- 反馈实际写入用户指定载体后，才能报告“已写入”。过程、失败尝试和阶段结论统一记录在项目 `进展.md`，不写入本 Skill。
