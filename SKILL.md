---
name: oa-baoxiao
description: 差旅费报销自动化。用于识别铁路电子客票、滴滴电子发票及行程单，填写树根互联 OA 差旅费报销单；明确触发「差旅报销问题反馈」时整理脱敏问题反馈。
agent_created: true
---

# oa-baoxiao

## 触发规则

- 报销任务：用户消息包含完整触发词「提交差旅报销」，或明确要求识别票据并填写 OA 报销单。旧触发词「火车票报销」不再作为主触发词。
- 反馈任务：仅在本 Skill 已匹配且消息包含完整短语「差旅报销问题反馈」时触发。
- 反馈任务不得执行报销填单。

## 报销流程

1. 读取用户提供的票据文件，铁路电子客票优先解析嵌入 XML；其它票据使用对应解析器，不用 OCR 代替可用的结构化数据。
2. 每次运行前确认项目名称；不得静默使用默认项目。通过 `--project` 传入。
3. 扫描用户提供的目录，目录内所有非隐藏文件都纳入总数，不写死票据类型或数量。逐文件判断“真实发票”与“非发票附件”；只有确认是真实发票的文件进入发票清单，其余全部进入其它附件清单。
4. 上传前后必须核对数量：发票栏文件数 = 去重后的发票清单数 = 表单“发票张数”；目录总文件数不等于发票数。数量不一致时停止暂存并转人工复核。
5. **附件硬分流**：`__landray_filefd_invoice_att` 只上传真实、可识别发票；`__landray_filefd_other_att` 上传行程报销单、行程单、登机凭证、酒店水单、地图截图、Excel 行程表及其它非发票文件。非发票不得上传到发票栏。
6. 发票上传后等待 OA 自动解析并自动新增对应明细行；禁止重复点击“添加行”。仅检查系统带出的内容，发现错误时覆盖修正；需要拆分明细时才按业务规则补行。
7. 滴滴电子发票与行程单按订单号、日期+金额、日期+起讫地点依次匹配，合并为一条明细；行程单只作其它附件。开始/结束日期分别取行程报销单起止日期，单日则相同。
8. 酒店发票使用“住宿”费用类型；入住开始和结束日期以酒店水单为准，水单只上传其它附件。
9. 自驾发票文件名含“自驾发票”时只上传一次；读取同目录 Excel 行程表，按 Excel 行数形成明细，分别覆盖出发地、到达地、日期、公里数、原因和金额；所有拆分明细共用同一发票编号。自驾金额以 Excel 为准，校验明细合计不低于发票价税合计。
10. 机票发票号码可能与“发票号码”标签分离，须在标签之后提取首个 20 位号码。
11. 发票类型：火车票为“专票”；滴滴、机票、酒店、自驾等其它发票为“普票”。滴滴费用类型固定为“出租车/网约车费/滴滴”。
12. 自动化只暂存，不自动提交；只有获得明确“已点暂存”回执才报告成功；“本月资金计划编号”暂跳过并提醒用户补充。

## 常用命令

```bash
python install.py
python scripts/fill_oa_auto.py "/path/发票.pdf" --project "项目名"
python scripts/fill_oa_batch.py "/path/发票目录" --project "项目名"
```

可选参数：`--trip "关键字"`、`--headless`、`--no-save`。会话默认使用 `data/oa_state.json`，可用 `OA_STATE_FILE` 覆盖。滴滴票据建议使用批量模式，将电子发票与行程单放在同一批次中。

## 登录与兼容性

- SSO 登录后必须点击 OA 应用，直到任一标签页回到 `oa.irootech.com`；停在统一认证门户不算完成。
- 报销单使用 `domcontentloaded` 和较长超时。
- select2 需要真实点击触发加载；首行费用类型为空时等待并重试。
- 项目通过 Relation_Choose 选择：打开弹窗、输入项目名、按 Enter 搜索、选择匹配项并确认。
- macOS 优先复用已登录 Chrome 会话；失败时引导浏览器登录。
- 不保存或输出密码、Cookie、Token、身份证号、姓名、发票号码和完整行程。
- 滴滴原则上使用企业滴滴；个人网约车、跨市打车、未叫到企业滴滴、额度超标等情况必须提示补充凭证或特批，不得静默视为合规。
- 跨市打车需说明原因并按制度要求补充审批；有车辆补贴人员的常驻办公区域用车不得直接报销。

## 问题反馈

1. 提取日期、分类、现象、影响、复现条件、处理结论、待验证项和状态；缺失字段填“待确认”，不得编造。
2. 先脱敏，只保留可复现的技术现象。
3. 目标为用户明确授权且可写的反馈载体。当前默认目标为用户提供的 Notion 页面；若后续改用腾讯文档问卷，只填写用户指定的输入框，不自动提交。
4. 只有实际写入成功后才报告“已写入”；连接器显示已连接不等于写入成功。
5. 写入失败时明确说明未写入，不使用其他资料库替代。
6. 禁止上传原始对话、原始截图、会话文件、Cookie、Token、密码、个人绝对路径及个人身份信息。
7. 统一格式：`日期｜分类｜现象｜影响｜复现条件｜处理结论｜待验证｜状态`。

## 反馈边界

- Skill 只保留稳定规则、字段映射、命令和安全约束。
- 不记录某次用户交流、某次失败、设备码、临时链接、任务编号或调试过程。
- 每个项目的阶段进展、决策、失败尝试和待办写入项目目录的 `进展.md`，不写入本 Skill。
